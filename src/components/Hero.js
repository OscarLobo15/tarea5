import react from "react";


function Hero() {
    return(
        <section class="hero">
            <div class="contenido-hero">
                <h2 class="nombre">Grupo Los Mamaos</h2>
            </div>

        </section>

    );
}

export default Hero;